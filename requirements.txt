# ProductFactory — Python dependencies
# Website (Ubuntu VM) + Orchestrator (Windows localhost) share this file.
# Pin versions for reproducible deploys.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
jinja2==3.1.4
python-multipart==0.0.12    # form parsing

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0             # async PostgreSQL driver (website runtime)
psycopg2-binary==2.9.10     # sync PostgreSQL driver (Alembic migrations only)
alembic==1.14.0             # schema migrations

# HTTP client (poller → PM API, poller → GitHub API)
httpx==0.27.2

# Auth
passlib[bcrypt]==1.7.4      # password hashing (if upgrading from Basic Auth)

# Markdown rendering (progress.md viewer)
mistune==3.0.2

# Security
pydantic==2.9.2             # request/response validation (FastAPI uses this)
pydantic-settings==2.6.0

# LLM feature recommendations
anthropic>=0.40.0

# Product video generation (post-coder step)
Pillow>=10.0.0              # slide image creation
edge-tts>=6.1.0             # Microsoft Edge Neural TTS narration (async)
moviepy>=1.0.3              # video assembly (requires ffmpeg on PATH)

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
httpx                       # already above — used for TestClient too
